# Tec-Tac Framework 1.4.0 Patch

Target repository: `/opt/tec-tac` (`jvz007/tac-net-rep`) only.

This is a repo-specific patch package, not a System Updates replacement archive. It preserves the existing repository and applies Module Management v2 files in place.

Install:

```bash
unzip tec-tac-framework-1.4.0-patch.zip
cd tec-tac-framework-1.4.0-patch
sudo bash apply.sh
```

Apply this before the Tec-Tac UI 0.4.0 patch.
